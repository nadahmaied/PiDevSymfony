<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Créer un compte admin - VitalTech</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="min-h-screen bg-slate-950 flex items-center justify-center px-4">
<div class="w-full max-w-2xl">
    <div class="mb-8 text-center">
        <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-slate-900 border border-slate-700 text-emerald-400 shadow-lg shadow-emerald-500/30">
            <i class="fa-solid fa-user-gear text-2xl"></i>
        </div>
        <h1 class="mt-4 text-2xl font-bold text-white">Créer un compte administrateur</h1>
        <p class="mt-1 text-sm text-slate-400">Donnez accès au backoffice VitalTech à un nouveau responsable.</p>
    </div>

    <div class="bg-slate-900/80 backdrop-blur-sm rounded-2xl shadow-2xl shadow-black/60 p-6 border border-slate-800">
        {{ form_start(registrationForm) }}

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div class="md:col-span-2 space-y-1.5">
                {{ form_label(registrationForm.email, null, {'label_attr': {'class': 'block text-sm font-medium text-slate-100'}}) }}
                {{ form_widget(registrationForm.email, {
                    'attr': {
                        'class': 'w-full rounded-xl border border-slate-700 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/60 text-sm px-3 py-2.5 bg-slate-950/60 text-slate-50 placeholder-slate-500',
                        'placeholder': 'admin@example.com'
                    }
                }) }}
                <div class="text-xs text-rose-400">
                    {{ form_errors(registrationForm.email) }}
                </div>
            </div>

            <div class="space-y-1.5">
                {{ form_label(registrationForm.nom, null, {'label_attr': {'class': 'block text-sm font-medium text-slate-100'}}) }}
                {{ form_widget(registrationForm.nom, {
                    'attr': {
                        'class': 'w-full rounded-xl border border-slate-700 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/60 text-sm px-3 py-2.5 bg-slate-950/60 text-slate-50',
                        'placeholder': 'Nom'
                    }
                }) }}
                <div class="text-xs text-rose-400">
                    {{ form_errors(registrationForm.nom) }}
                </div>
            </div>

            <div class="space-y-1.5">
                {{ form_label(registrationForm.prenom, null, {'label_attr': {'class': 'block text-sm font-medium text-slate-100'}}) }}
                {{ form_widget(registrationForm.prenom, {
                    'attr': {
                        'class': 'w-full rounded-xl border border-slate-700 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/60 text-sm px-3 py-2.5 bg-slate-950/60 text-slate-50',
                        'placeholder': 'Prénom'
                    }
                }) }}
                <div class="text-xs text-rose-400">
                    {{ form_errors(registrationForm.prenom) }}
                </div>
            </div>

            <div class="md:col-span-2 space-y-1.5">
                {{ form_label(registrationForm.adresse, null, {'label_attr': {'class': 'block text-sm font-medium text-slate-100'}}) }}
                {{ form_widget(registrationForm.adresse, {
                    'attr': {
                        'class': 'w-full rounded-xl border border-slate-700 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/60 text-sm px-3 py-2.5 bg-slate-950/60 text-slate-50',
                        'placeholder': 'Adresse (optionnel)'
                    }
                }) }}
                <div class="text-xs text-rose-400">
                    {{ form_errors(registrationForm.adresse) }}
                </div>
            </div>

            <div class="md:col-span-2 space-y-1.5">
                {{ form_label(registrationForm.telephone, null, {'label_attr': {'class': 'block text-sm font-medium text-slate-100'}}) }}
                {{ form_widget(registrationForm.telephone, {
                    'attr': {
                        'class': 'w-full rounded-xl border border-slate-700 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/60 text-sm px-3 py-2.5 bg-slate-950/60 text-slate-50',
                        'placeholder': 'Téléphone (optionnel)'
                    }
                }) }}
                <div class="text-xs text-rose-400">
                    {{ form_errors(registrationForm.telephone) }}
                </div>
            </div>

            <div class="md:col-span-2 space-y-1.5">
                {{ form_label(registrationForm.plainPassword, null, {'label_attr': {'class': 'block text-sm font-medium text-slate-100'}}) }}
                {{ form_widget(registrationForm.plainPassword, {
                    'attr': {
                        'class': 'w-full rounded-xl border border-slate-700 focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/60 text-sm px-3 py-2.5 bg-slate-950/60 text-slate-50',
                        'placeholder': 'Mot de passe administrateur'
                    }
                }) }}
                <p class="text-[11px] text-slate-500">Minimum 6 caractères, gardez-le confidentiel.</p>
                <div class="text-xs text-rose-400">
                    {{ form_errors(registrationForm.plainPassword) }}
                </div>
            </div>
        </div>

        <button
            type="submit"
            class="mt-6 w-full inline-flex justify-center items-center px-4 py-2.5 rounded-xl text-sm font-semibold text-slate-950 bg-emerald-400 hover:bg-emerald-300 shadow-sm shadow-emerald-500/40 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-slate-950 focus:ring-emerald-400 transition-colors"
        >
            <i class="fa-solid fa-user-check mr-2 text-xs"></i>
            Créer le compte administrateur
        </button>

        {{ form_end(registrationForm) }}

        <p class="mt-4 text-center text-xs text-slate-500">
            Vous avez déjà un compte administrateur ?
            <a href="{{ path('admin_login') }}" class="font-medium text-emerald-400 hover:text-emerald-300 hover:underline">
                Se connecter
            </a>
        </p>
    </div>

    <p class="mt-6 text-center text-[11px] text-slate-500">
        Retour à l'espace patient ?
        <a href="{{ path('front_login') }}" class="font-medium text-slate-200 hover:text-white underline">
            Connexion patient
        </a>
    </p>
</div>
</body>
</html>

