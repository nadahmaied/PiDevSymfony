<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion administrateur - VitalTech</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="min-h-screen bg-slate-950 flex items-center justify-center px-4">
<div class="w-full max-w-md">
    <div class="mb-8 text-center">
        <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-slate-900 border border-slate-700 text-sky-400 shadow-lg shadow-sky-500/30">
            <i class="fa-solid fa-shield-halved text-2xl"></i>
        </div>
        <h1 class="mt-4 text-2xl font-bold text-white">Connexion administrateur</h1>
        <p class="mt-1 text-sm text-slate-400">Gérez les utilisateurs et le backoffice VitalTech.</p>
    </div>

    <div class="bg-slate-900/80 backdrop-blur-sm rounded-2xl shadow-2xl shadow-black/60 p-6 border border-slate-800">
        {% if error %}
            <div class="mb-4 rounded-xl bg-rose-950/60 border border-rose-500/40 text-rose-100 px-4 py-3 text-sm flex items-start gap-2">
                <i class="fa-solid fa-circle-exclamation mt-0.5 text-rose-400"></i>
                <span>Identifiants invalides, veuillez réessayer.</span>
            </div>
        {% endif %}

        {% for message in app.flashes('success') %}
            <div class="mb-4 rounded-xl bg-emerald-950/60 border border-emerald-500/40 text-emerald-100 px-4 py-3 text-sm flex items-start gap-2">
                <i class="fa-solid fa-circle-check mt-0.5 text-emerald-400"></i>
                <span>{{ message }}</span>
            </div>
        {% endfor %}

        <form method="post" action="{{ path('front_login') }}" class="space-y-5">
            <div class="space-y-1.5">
                <label for="adminEmail" class="block text-sm font-medium text-slate-100">Email administrateur</label>
                <div class="relative">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-slate-500">
                        <i class="fa-solid fa-user-shield text-xs"></i>
                    </span>
                    <input
                        type="email"
                        id="adminEmail"
                        name="_username"
                        value="{{ last_username }}"
                        required
                        autofocus
                        class="w-full pl-9 pr-3 py-2.5 rounded-xl border border-slate-700 text-sm text-slate-50 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-sky-500 bg-slate-950/60"
                        placeholder="admin@example.com"
                    >
                </div>
            </div>

            <div class="space-y-1.5">
                <label for="adminPassword" class="block text-sm font-medium text-slate-100">Mot de passe</label>
                <div class="relative">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-slate-500">
                        <i class="fa-solid fa-lock text-xs"></i>
                    </span>
                    <input
                        type="password"
                        id="adminPassword"
                        name="_password"
                        required
                        class="w-full pl-9 pr-3 py-2.5 rounded-xl border border-slate-700 text-sm text-slate-50 placeholder-slate-500 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-sky-500 bg-slate-950/60"
                        placeholder="Mot de passe administrateur"
                    >
                </div>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            <input type="hidden" name="_target_path" value="{{ path('admin_user_index') }}">

            <button
                type="submit"
                class="w-full inline-flex justify-center items-center px-4 py-2.5 rounded-xl text-sm font-semibold text-white bg-sky-600 hover:bg-sky-500 shadow-sm shadow-sky-500/40 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-slate-950 focus:ring-sky-500 transition-colors"
            >
                <i class="fa-solid fa-right-to-bracket mr-2 text-xs"></i>
                Se connecter au backoffice
            </button>
        </form>

        <p class="mt-4 text-center text-xs text-slate-500">
            Besoin d'un compte administrateur ?
            <a href="{{ path('admin_register') }}" class="font-medium text-sky-400 hover:text-sky-300 hover:underline">
                Créer un compte admin
            </a>
        </p>
    </div>

    <p class="mt-6 text-center text-[11px] text-slate-500">
        Retour à l'espace patient ?
        <a href="{{ path('front_login') }}" class="font-medium text-slate-200 hover:text-white underline">
            Connexion patient
        </a>
    </p>
</div>
</body>
</html>

