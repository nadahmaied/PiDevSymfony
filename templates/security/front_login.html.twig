<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion - VitalTech</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-sky-50 via-white to-indigo-50 flex items-center justify-center px-4">
<div class="w-full max-w-md">
    <div class="mb-8 text-center">
        <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-sky-600 text-white shadow-lg shadow-sky-300/60">
            <i class="fa-solid fa-heart-pulse text-2xl"></i>
        </div>
        <h1 class="mt-4 text-2xl font-bold text-slate-900">Connexion à VitalTech</h1>
        <p class="mt-1 text-sm text-slate-500">Accédez à votre espace patient sécurisé.</p>
    </div>

    <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl shadow-sky-100/60 p-6 border border-slate-100">
        {% if error %}
            <div class="mb-4 rounded-xl bg-rose-50 border border-rose-200 text-rose-700 px-4 py-3 text-sm flex items-start gap-2">
                <i class="fa-solid fa-circle-exclamation mt-0.5"></i>
                <span>Identifiants invalides, veuillez réessayer.</span>
            </div>
        {% endif %}

        {% for message in app.flashes('success') %}
            <div class="mb-4 rounded-xl bg-emerald-50 border border-emerald-200 text-emerald-700 px-4 py-3 text-sm flex items-start gap-2">
                <i class="fa-solid fa-circle-check mt-0.5"></i>
                <span>{{ message }}</span>
            </div>
        {% endfor %}

        <form method="post" action="{{ path('front_login') }}" class="space-y-5">
            <div class="space-y-1.5">
                <label for="inputEmail" class="block text-sm font-medium text-slate-700">Adresse email</label>
                <div class="relative">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-slate-400">
                        <i class="fa-solid fa-envelope text-xs"></i>
                    </span>
                    <input
                        type="email"
                        id="inputEmail"
                        name="_username"
                        value="{{ last_username }}"
                        required
                        autofocus
                        class="w-full pl-9 pr-3 py-2.5 rounded-xl border border-slate-200 text-sm text-slate-900 placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-sky-500 bg-white"
                        placeholder="vous@example.com"
                    >
                </div>
            </div>

            <div class="space-y-1.5">
                <label for="inputPassword" class="block text-sm font-medium text-slate-700">Mot de passe</label>
                <div class="relative">
                    <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-slate-400">
                        <i class="fa-solid fa-lock text-xs"></i>
                    </span>
                    <input
                        type="password"
                        id="inputPassword"
                        name="_password"
                        required
                        class="w-full pl-9 pr-3 py-2.5 rounded-xl border border-slate-200 text-sm text-slate-900 placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-sky-500 focus:border-sky-500 bg-white"
                        placeholder="Votre mot de passe"
                    >
                </div>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <button
                type="submit"
                class="w-full inline-flex justify-center items-center px-4 py-2.5 rounded-xl text-sm font-semibold text-white bg-sky-600 hover:bg-sky-700 shadow-sm shadow-sky-300/60 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-sky-500 transition-colors"
            >
                <i class="fa-solid fa-right-to-bracket mr-2 text-xs"></i>
                Se connecter
            </button>
        </form>

        <p class="mt-4 text-center text-xs text-slate-500">
            Pas encore de compte ?
            <a href="{{ path('front_register') }}" class="font-medium text-sky-600 hover:text-sky-700 hover:underline">
                Créer un compte patient
            </a>
        </p>
    </div>

    <p class="mt-6 text-center text-[11px] text-slate-400">
        Accès administrateur ?
        <a href="{{ path('admin_login') }}" class="font-medium text-slate-600 hover:text-slate-800 underline">
            Se connecter au backoffice
        </a>
    </p>
</div>
</body>
</html>

