<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe oublié - VitalTech</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-sky-50 via-white to-indigo-50 flex items-center justify-center px-4">
<div class="w-full max-w-md">
    <div class="mb-8 text-center">
        <div class="inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-sky-600 text-white shadow-lg shadow-sky-300/60">
            <i class="fa-solid fa-key text-2xl"></i>
        </div>
        <h1 class="mt-4 text-2xl font-bold text-slate-900">Mot de passe oublié</h1>
        <p class="mt-1 text-sm text-slate-500">Saisissez votre adresse email pour recevoir un lien de réinitialisation.</p>
    </div>

    <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl shadow-sky-100/60 p-6 border border-slate-100">
        {{ form_start(requestForm) }}

        <div class="space-y-1.5">
            {{ form_label(requestForm.email, null, {'label_attr': {'class': 'block text-sm font-medium text-slate-700'}}) }}
            {{ form_widget(requestForm.email, {
                'attr': {
                    'class': 'w-full rounded-xl border border-slate-200 focus:border-sky-500 focus:ring-2 focus:ring-sky-500/60 text-sm px-3 py-2.5 bg-white',
                    'placeholder': 'vous@example.com'
                }
            }) }}
            <div class="text-xs text-rose-500">
                {{ form_errors(requestForm.email) }}
            </div>
        </div>

        <button
            type="submit"
            class="mt-6 w-full inline-flex justify-center items-center px-4 py-2.5 rounded-xl text-sm font-semibold text-white bg-sky-600 hover:bg-sky-700 shadow-sm shadow-sky-300/60 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-sky-500 transition-colors"
        >
            <i class="fa-solid fa-paper-plane mr-2 text-xs"></i>
            Envoyer le lien
        </button>

        {{ form_end(requestForm) }}

        <p class="mt-4 text-center text-xs text-slate-500">
            Vous vous souvenez de votre mot de passe ?
            <a href="{{ path('front_login') }}" class="font-medium text-sky-600 hover:text-sky-700 hover:underline">
                Retour à la connexion
            </a>
        </p>
    </div>
</div>
</body>
</html>

