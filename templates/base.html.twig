<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}HealthTrack{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
        .hover-scale { transition: transform 0.2s ease-in-out; }
        .hover-scale:hover { transform: translateY(-2px); }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-slate-50 text-slate-800">
    <div class="flex h-screen overflow-hidden">
        
        <!-- Sidebar -->
        <aside class="w-64 bg-white border-r border-slate-200 hidden md:flex flex-col z-20">
            <div class="p-6 flex items-center justify-center border-b border-slate-100">
                <div class="flex items-center gap-2 text-blue-600 font-bold text-2xl tracking-tight">
                    <i class="fa-solid fa-heart-pulse"></i><span>HealthTrack</span>
                </div>
            </div>
            <nav class="flex-1 p-4 space-y-2">
                <p class="text-xs font-bold text-slate-400 uppercase tracking-wider mb-2 pl-3">Menu Principal</p>
                <a href="{{ path('app_home') }}" class="flex items-center gap-3 px-4 py-3 text-slate-500 hover:bg-slate-50 hover:text-blue-600 rounded-xl font-medium transition duration-200">
                    <i class="fa-solid fa-chart-line w-5"></i> Tableau de Bord
                </a>
                
                <p class="text-xs font-bold text-slate-400 uppercase tracking-wider mt-8 mb-2 pl-3">Communauté</p>
                <a href="{{ path('app_donation_index') }}" class="flex items-center gap-3 px-4 py-3 {% if app.request.get('_route') starts with 'app_donation' %}text-orange-600 bg-orange-50 border border-orange-100{% else %}text-slate-500 hover:bg-slate-50 hover:text-orange-600{% endif %} rounded-xl font-medium transition shadow-sm">
                    <i class="fa-solid fa-hand-holding-heart w-5"></i> Dons & Annonces
                </a>
            </nav>
            <div class="p-4 border-t border-slate-100">
                <button class="flex items-center gap-3 px-4 py-2 w-full text-red-500 hover:bg-red-50 rounded-lg transition text-sm font-medium">
                    <i class="fa-solid fa-right-from-bracket"></i> Déconnexion
                </button>
            </div>
        </aside>

        <main class="flex-1 flex flex-col h-screen overflow-hidden relative">
            
            <!-- Header -->
            <header class="bg-white/80 backdrop-blur-md border-b border-slate-200 py-4 px-8 flex justify-between items-center shrink-0 z-10 sticky top-0">
                <h1 class="text-2xl font-bold text-slate-800">{% block page_title %}Espace Solidarité{% endblock %}</h1>
                <div class="flex items-center gap-4">
                    <button class="p-2 text-slate-400 hover:text-orange-600 transition relative">
                        <i class="fa-regular fa-bell text-xl"></i>
                        <span class="absolute top-1 right-1 w-2.5 h-2.5 bg-red-500 rounded-full border-2 border-white"></span>
                    </button>
                    <div class="flex items-center gap-3 hover:bg-slate-50 p-1.5 rounded-lg transition border border-transparent hover:border-slate-200">
                        <div class="text-right hidden sm:block leading-tight">
                            <p class="text-sm font-bold text-slate-700">Utilisateur</p>
                            <p class="text-xs text-orange-500">Bénévole Actif</p>
                        </div>
                        <img src="https://ui-avatars.com/api/?name=User&background=f97316&color=fff" class="w-10 h-10 rounded-full object-cover shadow-sm">
                    </div>
                </div>
            </header>

            <!-- Main Content -->
            <div class="flex-1 overflow-y-auto bg-slate-50 p-4 md:p-8 no-scrollbar">
                <div class="max-w-7xl mx-auto space-y-8">
                    {% block body %}{% endblock %}
                </div>
            </div>
        </main>
    </div>

    {% block javascripts %}{% endblock %}
</body>
</html>
