<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin - HealthTrack{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100 font-sans">
    <div class="flex h-screen overflow-hidden">
        
        <aside class="w-64 bg-slate-900 text-white flex flex-col fixed h-full z-10 lg:static">
            <div class="p-6 flex items-center justify-center border-b border-slate-800">
                <div class="flex items-center gap-2 font-bold text-2xl tracking-wider">
                    <i class="fa-solid fa-shield-virus text-blue-500"></i><span>ADMIN</span>
                </div>
            </div>
            
            <nav class="flex-1 p-4 space-y-2 text-sm overflow-y-auto">
                <a href="{{ path('app_home') }}" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:bg-slate-800 hover:text-white rounded-xl transition">
                    <i class="fa-solid fa-gauge w-5"></i> Dashboard
                </a>

                <a href="{{ path('admin_user_index') }}" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:bg-slate-800 hover:text-white rounded-xl transition">
                    <i class="fa-solid fa-users w-5"></i> Utilisateurs
                </a>
                <a href="{{ path('account_show') }}" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:bg-slate-800 hover:text-white rounded-xl transition">
                    <i class="fa-solid fa-user-gear w-5"></i> Mon compte
                </a>

                <p class="text-xs font-bold text-slate-500 uppercase mt-4 mb-2 pl-3">Gestion</p>

                <a href="{{ path('app_admin_missions_index') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl font-medium transition {% if app.request.get('_route') starts with 'app_admin_missions' %}bg-orange-600 text-white shadow-lg shadow-orange-900/50{% else %}text-slate-400 hover:bg-slate-800 hover:text-white{% endif %}">
                    <i class="fa-solid fa-hand-holding-heart w-5"></i> Bénévolat
                </a>

                <a href="{{ path('admin_annonce_index') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl font-medium transition {% if app.request.get('_route') starts with 'admin_annonce' %}bg-blue-600 text-white shadow-lg shadow-blue-900/50{% else %}text-slate-400 hover:bg-slate-800 hover:text-white{% endif %}">
                    <i class="fa-solid fa-bullhorn w-5"></i> Gestion Annonces
                </a>

                <a href="{{ path('admin_donation_index') }}" class="flex items-center gap-3 px-4 py-3 rounded-xl font-medium transition {% if app.request.get('_route') starts with 'admin_donation' %}bg-emerald-600 text-white shadow-lg shadow-emerald-900/50{% else %}text-slate-400 hover:bg-slate-800 hover:text-white{% endif %}">
                    <i class="fa-solid fa-hand-holding-heart w-5"></i> Gestion Donations
                </a>

                <a href="#" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:bg-slate-800 hover:text-white rounded-xl transition">
                    <i class="fa-solid fa-calendar-day w-5"></i> Forum
                </a>
                <a href="{{ path('front_logout') }}" class="flex items-center gap-3 px-4 py-3 text-red-400 hover:bg-slate-800 hover:text-red-300 rounded-xl transition">
                    <i class="fa-solid fa-right-from-bracket w-5"></i> Déconnexion
                </a>
            </nav>
        </aside>

        <main class="flex-1 flex flex-col overflow-hidden relative">
            {% block body %}{% endblock %}
        </main>
    </div>
    {% block javascripts %}{% endblock %}
</body>
</html>